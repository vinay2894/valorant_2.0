import 'package:flutter/material.dart';

class GunDetail extends StatelessWidget {
  const GunDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
